# Legacy audit build workspace

This opaque directory is retained only because its `work/node_modules` junction provides the local
artifact runtime used by `work/build_retrieval_audit.mjs`. It does not contain result files.

The organized audit results are in `outputs/audits/three_model_retrieval_audit/`.

